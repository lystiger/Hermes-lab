# Sprint 03 — Agent adapter implementation

Implement the Sprint 03 brief in `docs/HERMES_LAB_SPRINT_03_AGENT_ABSTRACTION_CODEX.md`.

Keep agent CLI behavior behind the adapter interface and registry. Preserve the controller's exclusive ownership of worktree validation, Git staging, commits, merges, integration tests, run summaries, and final status. Preserve Sprint 02 Antigravity and Claude behavior, add the non-interactive Codex CLI adapter, add focused unit tests, and write `HANDOFF_CLAUDE.md`.

Do not commit, merge, rebase, push, edit Git metadata, or operate outside this assigned worktree. The controller owns all Git operations.
