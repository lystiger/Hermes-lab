# Sprint 03 — Independent adapter verification

Review the integrated Sprint 03 implementation against `docs/HERMES_LAB_SPRINT_03_AGENT_ABSTRACTION_CODEX.md`.

Run the full test suite and inspect registry lookup, command construction, non-zero exits, timeouts, malformed output, missing executables, Codex output logs, worktree validation, handoff enforcement, Git ownership, merge ownership, and the integration test gate. Add focused regression tests or narrowly supported fixes only. Write `HANDOFF_CODEX.md` with findings and test results.

Do not commit, merge, rebase, push, edit Git metadata, or operate outside this assigned worktree. The controller owns all Git operations.
