# Sprint 04 — Execution backend implementation

Implement the backend abstraction and one-shot Herdr runtime described in `docs/HERMES_LAB_SPRINT_04_HERDR_BACKEND.md`.

Use the subprocess backend for this bootstrap phase. Preserve every Sprint 03.1 controller, validation, worker-sandbox, deterministic-reset, and sanitized-report invariant. Add focused tests and update `HANDOFF_CLAUDE.md`.

Do not commit, merge, rebase, push, edit Git metadata, or operate outside this assigned worktree. The controller owns all Git operations.
