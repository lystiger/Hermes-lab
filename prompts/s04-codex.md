# Sprint 04 — Herdr backend verification

Independently verify the integrated Sprint 04 implementation through the Herdr backend. Focus on subprocess parity, JSON protocol parsing, opaque runtime IDs, wrapper injection resistance, completion nonces, timeouts, partial logs, targeted interruption, runner-owned cleanup, backend selection, and preserved controller gates.

Run the complete suite, make only evidence-supported fixes, and update `HANDOFF_CODEX.md` with test and Herdr dogfood results.

Do not commit, merge, rebase, push, edit Git metadata, or operate outside this assigned worktree. The controller owns all Git operations.
